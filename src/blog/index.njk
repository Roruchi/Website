---
layout: base.njk
title: Writing
description: Articles on engineering, AI, leadership, human growth, and the practices that help people build with clarity and care.
permalink: /blog/index.html
tags: []
eleventyExcludeFromCollections: true
---

<section class="page-hero">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-20 md:py-28">
    <div class="eyebrow mb-6">The writing garden</div>
    <h1>Notes from the intersection of code and consciousness.</h1>
    <p class="page-lead">I write about engineering, AI, leadership, and human growth: how disciplined technology and honest attention can help people make better decisions and do better work together.</p>
  </div>
</section>

<section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
  <div class="blog-grid">
    {%- for post in collections.post | reverse -%}
    <article class="blog-card">
      <a href="{{ post.url }}" class="blog-card-image">
        <img src="{{ post.data.cover }}" alt="" loading="lazy">
        <span>{{ post.data.pillar }}</span>
      </a>
      <div class="blog-card-body">
        <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        <p>{{ post.data.description }}</p>
        <div class="blog-card-footer">
          <a href="{{ post.url }}" class="text-link">Continue reading</a>
          <span>{{ post.templateContent | readingTime }}</span>
        </div>
      </div>
    </article>
    {%- endfor -%}
  </div>
</section>
